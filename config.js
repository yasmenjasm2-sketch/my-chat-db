/**
 * 🛠️ ملف إعدادات الإعلان المتقدم - FawakihAI Ad System
 * يمكنك تعديل هذا الملف من GitHub مباشرة لتغيير الإعلان في التطبيق فوراً.
 */

const adConfig = {
    // 1. حالة الإعلان العامة
    // (true): الإعلان يظهر للمستخدمين | (false): إخفاء الإعلان تماماً
    isActive: true,

    // 2. نوع المحتوى الإعلاني
    // "image": لعرض صورة ثابتة | "video": لعرض مقطع فيديو (MP4)
    type: "video",

    // 3. مسار ملف الميديا
    // ضع اسم الملف المرفوع في GitHub أو رابطاً مباشراً (مثل: ad_video.mp4 أو promo.png)
    fileName: "hamza1.mp4",

    // 4. رابط التوجه عند النقر
    // الرابط الذي سيفتح في المتصفح عندما يضغط المستخدم على الإعلان
    link: "https://wsend.co/905424678123",

    // 5. إعدادات وقت العرض (للمعلومات فقط)
    // ملاحظة: النظام مبرمج الآن ليغلق الإعلان تلقائياً بعد 12 ثانية كحد أقصى.
    maxDuration: 12, 
    
    // تاريخ آخر تحديث قمت به
    lastUpdated: "2026-05-11"
};

// ملاحظات للمطور (Remas):
// - إذا كان النوع "video"، تأكد أن حجم الفيديو صغير لضمان سرعة التحميل.
// - يفضل استخدام صيغة MP4 للفيديوهات وصيغة PNG أو JPG للصور.
// - زر التخطي يظهر تلقائياً بعد 5 ثوانٍ، والإغلاق الكلي بعد 12 ثانية.
